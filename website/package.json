{
  "name": "quickred-differentiator-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate:seo": "node scripts/generate-seo.mjs",
    "dev": "npm run generate:seo && vite",
    "build": "npm run generate:seo && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^5.4.12"
  }
}
